# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: telemetry.spec.ts >> Telemetry Section Tests >> Metrics (Prometheus) page loads
- Location: tests/e2e/telemetry.spec.ts:37:7

# Error details

```
Error: expect(page).toHaveURL(expected) failed

Expected pattern: /telemetry\/metrics/i
Received string:  "http://localhost:9081/telemetry/charts"
Timeout: 60000ms

Call log:
  - Expect "toHaveURL" with timeout 60000ms
    - unexpected value "http://localhost:9081/telemetry"
    109 × unexpected value "http://localhost:9081/telemetry/charts"

```

```yaml
- navigation:
  - img
  - img
  - list:
    - link "Dashboard":
      - /url: /
      - img
      - text: Dashboard
    - link "Lifecycle Expand":
      - /url: /management/connections
      - img
      - text: Lifecycle
      - button "Expand"
    - link "Configuration Expand":
      - /url: /configuration/designs
      - img
      - text: Configuration
      - button "Expand"
    - link "Telemetry Collapse":
      - /url: /telemetry
      - img
      - text: Telemetry
      - button "Collapse" [expanded]
    - list:
      - button "Charts":
        - img
        - text: Charts
      - button "Metrics":
        - img
        - text: Metrics
    - link "meshery-button-2 Performance Expand":
      - /url: /performance
      - img "meshery-button-2"
      - text: Performance
      - button "Expand"
    - link "Extensions":
      - /url: /extensions
      - img
      - text: Extensions
    - separator
  - img
  - group:
    - listitem:
      - link:
        - /url: https://docs.meshery.io
        - img
    - listitem:
      - link:
        - /url: https://slack.meshery.io
        - img
    - listitem:
      - link:
        - /url: https://meshery.io/community#community-forums
        - img
    - listitem:
      - link:
        - /url: https://github.com/meshery/meshery/issues/new/choose
        - img
  - listitem:
    - text: undefined
    - link:
      - /url: https://docs.meshery.io/project/releases/undefined
      - img
    - text: Update available
    - link:
      - /url: https://docs.meshery.io/project/releases/v1.0.56
      - img
- banner:
  - button:
    - img
  - text: /
  - button:
    - img
  - text: /
  - heading "Charts" [level=5]
  - button "contexts":
    - img
    - text: "0"
  - button:
    - img
  - button
  - button:
    - img
- main:
  - img
  - heading "No Grafana connections yet" [level=6]
  - paragraph: Add a Grafana connection to browse and render its dashboards here. Connections are managed from the Connections page.
  - button "Add a Grafana connection":
    - img
    - text: Add a Grafana connection
- contentinfo:
  - paragraph:
    - text: Built with
    - img
    - text: by the Meshery Community
- alert: Telemetry Charts | Meshery
```

# Test source

```ts
  1  | import { expect, test, Page } from '@playwright/test';
  2  | import { DashboardPage } from './pages/DashboardPage';
  3  | 
  4  | // Telemetry is the section that replaced the legacy "Configure Metrics" flow
  5  | // (Grafana API key under Settings > Metrics, and the Performance page's
  6  | // configure-metrics button). It now lives under /telemetry with two sub-pages:
  7  | //   - /telemetry/charts  -> Grafana dashboards
  8  | //   - /telemetry/metrics -> Prometheus metrics
  9  | // Both render an empty state when no matching connection is registered, which
  10 | // is the expected state in a fresh CI environment.
  11 | 
  12 | test.describe('Telemetry Section Tests', () => {
  13 |   // navigateToDashboard() + navigateToSubMenuItem() chain their own 120s inner
  14 |   // waits; 180s gives the hook enough wall-clock on slow CI.
  15 |   test.describe.configure({ timeout: 180_000 });
  16 | 
  17 |   test.beforeEach(async ({ page }: { page: Page }) => {
  18 |     const dashboardPage = new DashboardPage(page);
  19 |     await dashboardPage.navigateToDashboard();
  20 |   });
  21 | 
  22 |   test('Charts (Grafana) page loads', async ({ page }: { page: Page }) => {
  23 |     const dashboardPage = new DashboardPage(page);
  24 |     await dashboardPage.navigateToTelemetryCharts();
  25 | 
  26 |     await expect(page).toHaveURL(/telemetry\/charts/i);
  27 |     await expect(page.getByTestId('telemetry-charts')).toBeVisible();
  28 | 
  29 |     // Either the empty state (no Grafana connection registered, the CI default)
  30 |     // or the connection toolbar (an environment with a connection) is fine —
  31 |     // both prove the page mounted without crashing.
  32 |     await expect(
  33 |       page.getByTestId('telemetry-grafana-empty').or(page.getByTestId('telemetry-grafana-toolbar')),
  34 |     ).toBeVisible();
  35 |   });
  36 | 
  37 |   test('Metrics (Prometheus) page loads', async ({ page }: { page: Page }) => {
  38 |     const dashboardPage = new DashboardPage(page);
  39 |     await dashboardPage.navigateToTelemetryMetrics();
  40 | 
> 41 |     await expect(page).toHaveURL(/telemetry\/metrics/i);
     |                        ^ Error: expect(page).toHaveURL(expected) failed
  42 |     await expect(page.getByTestId('telemetry-metrics')).toBeVisible();
  43 | 
  44 |     await expect(
  45 |       page
  46 |         .getByTestId('telemetry-prometheus-empty')
  47 |         .or(page.getByTestId('telemetry-prometheus-toolbar')),
  48 |     ).toBeVisible();
  49 |   });
  50 | });
  51 | 
```